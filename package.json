{
  "name": "hhvm-www",
  "version": "0.0.0",
  "scripts": {
    "start": "hhvm -m server -p 8080",
    "build": "node scripts/build",
    "dev": "watch -p \"www/assets/**\" -c \"npm run build\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "clean-css": "^3.4.12",
    "del": "^2.2.0",
    "loader-utils": "^0.2.15",
    "requirejs": "^2.2.0",
    "uglify-js": "^2.6.2",
    "watch-cli": "^0.2.1"
  }
}
